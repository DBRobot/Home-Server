[package]
name = "account"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "A member's own identity from any device: create, admit, recover, redeem an invite"

[dependencies]
auth = { path = "../auth" }
identity = { path = "../identity" }
directory = { path = "../directory" }
library = { path = "../library" }
anyhow = "1"
base64 = "0.22"
ed25519-dalek = "2"
zeroize = "1"
